# LOCAL ROUTE 문서 검토 보고서

| 항목 | 내용 |
| --- | --- |
| 검토 대상 | `LOCAL_ROUTE_기획서_v5.md` / `LOCAL_ROUTE_6인_개발계획서_및_업무지침서.md` (및 각 .docx) |
| 검토일 | 2026-08-26 |
| 검토 방법 | 문서 서술을 저장소 코드·개발 DB·공식 문서와 1:1 대조 |
| 결과 | **치명 4건 · 중대 11건 · 보완 17건** |
| 조치 | 두 문서를 v6으로 전면 개정. 본 보고서는 무엇을 왜 고쳤는지의 기록 |

---

## 0. 총평

두 문서는 방향과 구성이 좋다. 기능 정의가 구체적이고, 6인 역할이 명확하며, 스토어 심사 요건을 미리 챙긴 점이 특히 좋다.

문제는 **검증되지 않은 수치와 계획이 사실처럼 적혀 있다는 것**이다. 청사진으로 쓰려면 이 부분이 위험하다. 팀원이 문서를 믿고 작업했는데 실제 코드가 다르면 그 시간이 전부 낭비된다.

가장 심각한 것은 **일정을 실제로 막는 두 가지가 누락**된 점이다.

1. Google Play가 2026-08-31부터 신규 앱에 **target API 36**을 요구한다. 문서는 34로 적혀 있다. 이대로면 제출 자체가 거부된다.
2. 신규 개인 개발자 계정은 **테스터 12명 × 14일 비공개 테스트**를 마쳐야 프로덕션 출시가 가능하다. 문서에 이 규칙이 없어, "9/11 제출 = 9월 출시"로 읽힌다. 실제로는 10월 초가 된다.

---

## 1. 치명 — 그대로 두면 출시가 막힌다

### C-1. Target SDK 34는 제출 불가 [확정 사실]

- **문서 서술**: 기획서 §11.2 "Target SDK 34(Android 14) 충족"
- **실제**: Google Play는 **2026년 8월 31일부터 신규 앱과 업데이트에 target API level 36 (Android 16)** 을 요구한다. 기존 앱이 새 사용자에게 계속 노출되려면 API 35가 필요하다.
- **영향**: 9월 제출 시 API 34 빌드는 업로드가 거부된다.
- **수정**: `targetSdkVersion 36` / `compileSdkVersion 36`. Expo SDK가 API 36을 지원하는 버전인지 **8/27까지 확인**하고, 미지원이면 Expo 버전을 올리거나 `expo-build-properties`로 명시 지정한다.
- 연장 신청(최대 2026-11-01)은 가능하나 출시일이 밀리므로 대안으로만 둔다.

> 출처: Android Developers 「Target API level requirements for Google Play apps」 (2026-08-26 확인)

### C-2. Play 비공개 테스트 12명·14일 규칙 누락 [확정 사실]

- **문서 서술**: 지침서 §3 "9/11 Google Play & App Store 최종 심사 제출" → 9월 출시로 읽힌다.
- **실제**: 2023-11-13 이후 생성된 **개인** Play Console 계정은 테스터 **최소 12명**이 **연속 14일** 참여하는 비공개 테스트를 마쳐야 프로덕션 액세스를 신청할 수 있고, 그 신청 검토에 **최대 7일**이 더 걸린다.
- **영향**: 9/11에 테스트를 시작하면 9/25 요건 충족 → **10월 초 출시**.
- **수정**: 비공개 테스트를 **8/31에 껍데기 빌드로 먼저 시작**한다. 설치되고 크래시 없이 켜지면 되므로 완성품일 필요가 없다. 그러면 9/14 요건 충족 → **9/21 전후 Play 출시**가 가능하다.
- 아울러 **테스터 12명을 8/28까지 확보**하는 것을 별도 작업으로 세운다. 문서에 이 항목이 아예 없다.

### C-3. 소셜 로그인 추가가 iOS 심사 요건을 새로 만든다 [확정 사실]

- **문서 서술**: 지침서 §2.4 "소셜 로그인(카카오/구글/애플) OAuth 2.0", 1단계(~9/4) 업무로 배정
- **실제**: App Store 지침 **4.8**은 서드파티·소셜 로그인을 쓰면 동등한 대안 로그인을 함께 제공하도록 요구한다. 다만 **"자사 계정 시스템만 사용하는 앱"은 예외**다.
- **현재 상태**: 이메일 + `scrypt` 자체 계정만 있다 → **지금은 4.8 대상이 아니다.**
- **영향**: 카카오·구글 로그인을 추가하는 순간 4.8이 발동해 Sign in with Apple 구현이 사실상 필요해진다. 9일 일정에 심사 리스크를 스스로 만드는 셈이다.
- **수정**: **소셜 로그인은 v1.1로 미룬다.** 1단계는 기존 이메일 계정을 다듬는 데 집중한다.

> 출처: Apple App Store Review Guidelines 4.8 (2026-08-26 확인)

### C-4. 계정 삭제 API가 없는데 있는 것처럼 읽힌다 [실측]

- **문서 서술**: 기획서 §8 `routes/account.ts` **8개**, §11.1 `DELETE /api/account/me` 구현
- **실제**: `account.ts`의 엔드포인트는 **5개**다. `POST /auth/register`, `POST /auth/login`, `GET /auth/me`, `PATCH /auth/me`, `POST /auth/logout`. **삭제 API는 존재하지 않는다.**
- **실제 요건**: 지침 **5.1.1(v)** — "계정 생성을 지원하면 앱 내에서 계정 삭제도 제공해야 한다". 회피할 수 없다.
- **수정**: 미구현임을 명시하고 **1단계(9/4)** 작업으로 앞당긴다. 2단계로 두면 앱 제출 직전에 몰린다. 웹에도 같은 경로가 필요하다.

---

## 2. 중대 — 사실과 다르거나 실행을 오도한다

### M-1. API 배분표가 실제와 거의 모든 행이 다르다 [실측]

합계 66만 우연히 맞다.

| 라우터 | 문서 | 실제 | 차이 |
| --- | ---: | ---: | ---: |
| `trips.ts` | 18 | **23** | +5 |
| `account.ts` | 8 | **5** | −3 |
| `community.ts` | 9 | **5** | −4 |
| `social.ts` | 8 | **9** | +1 |
| `experience.ts` | 7 | **5** | −2 |
| `collaboration.ts` | 6 | **7** | +1 |
| `commerce.ts` | 5 | **8** | +3 |
| `analytics.ts` | 5 | **3** | −2 |
| `index.ts` (`/health`) | 없음 | **1** | +1 |
| 합계 | 66 | **66** | — |

**수정**: 실제 수치로 교체하고, `trips.ts` 23개는 BE와 AI가 나눠 쓰는 구조를 명시했다.

### M-2. "부산 200곳 시드 데이터" — 이미 277곳이 있다 [실측]

- 개발 DB 실측: `Place` **277행**. 문서의 200곳 목표는 현재보다 적다.
- **진짜 문제는 개수가 아니라 결측 필드다**:

| 필드 | 채워짐 | 비율 |
| --- | ---: | ---: |
| `nameEn` (영문 장소명) | 42/277 | **15%** |
| `addressEn` (영문 주소) | 0/277 | **0%** |
| `allergens` (알레르기) | 0/277 | **0%** |
| `dietOptions` (식단) | 0/277 | **0%** |
| `closedDays` (휴무일) | 55/277 | 20% |
| `kakaoRating` 외 리뷰 6종 | 0/277 | **0%** |
| `PlaceTranslation` 행 | 36행 | 13% |

- 외국인 대상 서비스인데 영문 이름이 15%, 영문 주소가 0%다. 기획서 §5.11의 "택시 기사에게 보여줄 주소 카드"도, §5.9의 "카카오 평점 카드"도 **데이터가 없어 지금은 동작하지 않는다**.
- **수정**: DB 담당 목표를 "200곳 적재"에서 **"결측 필드 채우기"** 로 바꾸고, 필드별 목표치를 수용 기준으로 세웠다.

### M-3. 문서 어디에도 "지금 무엇이 가짜인가"가 없다

사용자가 "이 앱은 데모"라고 규정했는데, 두 문서 모두 현재 상태를 완성된 것처럼 서술한다. 실제로 확인한 데모 항목은 다음과 같다.

| 항목 | 실제 구현 | 근거 |
| --- | --- | --- |
| 날씨 예보 | 날짜 문자열의 문자코드 합으로 기온·강수확률 생성 | `routes/experience.ts:129` · `source: "CACHED_DEMO_FORECAST"` |
| 웹 날씨 위젯 | 서버 값조차 안 쓰고 3일치 하드코딩 | `WeatherPrepWidget.tsx:21` `const mockWeather = [...]` |
| 음성 통역 | `text.includes("pork")` 식 if-else 분기 | `VoiceTranslatorModal.tsx:130` 주석 `for demo` |
| 메뉴판 번역 | Tesseract.js + 수작업 사전 **113개 항목** | `utils/menuOcr.ts` |
| 스토리 이미지 | base64 Data URL을 **DB에** 저장 | `routes/social.ts:23` |
| 예약 제휴 | `https://example.com/booking/local-route-demo` | `ensure-commerce.ts:20` |
| 광고 캠페인 | 데모 8건이 실제처럼 노출 | DB `AdCampaign` 8행 |
| 관리자 토큰 | `.env` 미설정 → 기본값 `local-route-demo-admin` | `routes/social.ts:143` |
| 예약 웹훅 서명 | 미설정 → 기본값 `change-before-production` | `docker-compose.yml` |

**수정**: 기획서에 「현재 구현 실측과 데모 감사」 장을 신설했다. 청사진은 출발점을 정확히 적어야 한다.

### M-4. "1,200종 한국 음식 DB" — 근거가 없다

- 지침서 §2.1과 §6에 반복 등장하나, 현재 사전은 **113개 항목**이다.
- 1,200종을 어디서 얼마의 시간에 만드는지가 없다.
- **수정**: 목표를 **"부산 대표 메뉴 300종 + 알레르기 8대 원재료 매핑"** 으로 현실화하고, 만드는 방법(공공데이터 + 수작업 검수)과 담당·기한을 붙였다.

### M-5. 검증 불가능하거나 통제 불가능한 KPI

| 문서 KPI | 문제 | 수정 |
| --- | --- | --- |
| OCR 정확도 **95%** | Tesseract.js 한글 메뉴판에서 비현실적. 측정 기준도 없음 | 클라우드 OCR 전환을 전제로 **"부산 대표 메뉴 50종 사진에서 메뉴명 인식 80% 이상"**, 측정 스크립트 명시 |
| 음성 통역 지연 **1.2초** | 현재 번역이 if-else라 지연 개념 자체가 없음 | **"STT→번역→TTS 왕복 3초 이하, 실패 시 텍스트 입력으로 폴백"** |
| 이동시간 **35% 단축** | 비교 대상·측정 방법 없음 | **"최적화 미적용 대비 동일 조건 10회 평균 이동시간 비교, 결과를 그대로 기록"** (목표치를 미리 못 박지 않음) |
| 스토어 심사 **0 Reject** | 팀이 통제할 수 없음 | **"제출 전 리젝 방지 체크리스트 24항목 전수 통과"** 로 대체 |
| 크래시율 **0%** | 표본 없이 0%는 측정 불가 | **"심사 시나리오 12종 실기기 2회 반복 시 크래시 0건"** |

### M-6. 심사용 계정 비밀번호가 문서에 평문으로 있다

- 지침서 §5: `appstore_review@localroute.kr` / `Test1234!`
- 이 문서는 저장소에 커밋된다. **자격증명을 공개 저장소에 넣는 것과 같다.**
- **수정**: 문서에서 비밀번호를 삭제하고, "심사 계정 자격증명은 App Store Connect 심사 메모에만 입력하며 저장소에 두지 않는다"는 규칙으로 대체했다.

### M-7. AWS 풀스택 구성이 9일 일정에 맞지 않는다

- 문서: ECS Fargate + RDS + S3 + CloudFront + ALB + VPC
- 이 구성을 처음부터 세우는 데만 2~4일이 든다. INFRA 1명이 같은 기간에 CI/CD, HTTPS, 스토어 계정, EAS 빌드까지 해야 한다.
- **수정**: **단일 VM + Docker Compose + Nginx + Let's Encrypt**로 시작하고, 목표 아키텍처(관리형 DB·CDN)는 v1.1 이전 과제로 분리했다. 발표에서는 "현재 구성 + 확장 경로"로 설명하면 된다.
- 다만 **스토리 이미지 저장소만은 예외**다. DB에 base64로 쌓는 구조는 1단계에 반드시 걷어내야 하므로 오브젝트 스토리지를 우선 도입 대상으로 남겼다.

### M-8. PostGIS 도입이 불필요하고 자기모순이다

- 문서 §7 "PostgreSQL 16 + PostGIS"라 해놓고, 지침서 §2.2에는 "하버사인 최단 거리 공간 쿼리 인덱스(`lat`,`lng` 복합 인덱스)"라고 적혀 있다. 두 방식이 섞여 있다.
- 277~수천 건 규모에서는 **바운딩 박스 1차 필터 + 하버사인 정렬**로 충분하다. PostGIS는 확장 설치·학습 비용만 든다.
- **수정**: PostGIS를 v1.1 이후로 미루고, 인덱스 설계를 구체화했다.

### M-9. ERD가 실제 스키마와 다르다

- 문서 ERD: `TRIP_DAY`, `TRIP_STORY`, `PLACE_REVIEW`
- 실제 모델명: `ItineraryDay`, `Story`, `Review`
- 문서 ERD의 `Place.soloFriendly` 컬럼은 **실제 스키마에 없다**(Place는 42개 컬럼). 기획서 §5.4의 `SOLO_FRIENDLY` 코스와 원칙 2의 "혼밥 가능 여부"가 데이터로 뒷받침되지 않는다.
- **수정**: 실제 26개 모델명과 관계로 ERD를 다시 그리고, `soloFriendly`는 **1단계 추가 컬럼**으로 명시했다.

### M-10. 타임라인이 격일이라 실행 계획이 못 된다

- 지침서 §3: 8/27, 8/29, 9/01, 9/03, 9/04만 있다. **8/28·8/30·8/31·9/02가 없다.** 2단계도 9/06·9/08이 없다.
- 시작일이 **8/27**인데 오늘은 8/26이다. 하루를 버린다.
- 완충일이 하나도 없다. 토·일이 전부 작업일이다.
- **수정**: **8/26부터 9/11까지 하루도 빠짐없이**, 6인 각자의 그날 할 일과 완료 기준을 적었다. 완충 구간(8/30 일, 9/7 일)을 명시했다.

### M-11. 디자인 토큰 색상이 현재 UI와 다르다

- 문서 토큰: `primary: '#0f766e'` (딥 에메랄드/틸)
- 실제 UI: 보라 계열(`#6655c8`·`#7265c6`) + 흰 사이드바
- 이 토큰을 그대로 적용하면 **전면 리디자인**이 된다. 9일 일정에서 치명적이다.
- **수정**: 현재 화면에서 실제 사용 중인 색을 추출한 토큰으로 교체했다. 색을 바꾸고 싶다면 그건 별도 결정 사항이며 일정에 반영해야 한다.

---

## 3. 보완 — 있으면 훨씬 나아진다

| # | 항목 | 문제 | 수정 |
| --- | --- | --- | --- |
| B-1 | PR 머지 조건 | `npx tsc --noEmit`는 이 워크스페이스에서 동작하지 않는다(루트에 tsconfig 없음) | `npx tsc -b web` · `npm run build --workspace server` · `npm test --workspace server` · `npm audit --audit-level=high`로 교체 |
| B-2 | CI 반영 | 저장소에 이미 CI가 있는데 문서에 없다 | 실제 `ci.yml` 단계를 그대로 옮기고 추가할 항목 표시 |
| B-3 | 반응형 기준 | "375px"만 언급 | 실제 브레이크포인트가 **9종 난립**(520·640·680·720·760·840·900·1100·1101). 4종으로 표준화하는 규격과 검수 매트릭스 신설 |
| B-4 | 앱 화면 범위 | 44개 화면을 7일에 만든다는 전제 | 앱 v1 **12화면**으로 축소하고 나머지는 웹뷰. 근거와 목록 명시 |
| B-5 | 롤백 계획 | 없음 | 배포 전 백업·롤백 절차와 판단 기준 추가 |
| B-6 | 테스터 12명 | 확보 계획 없음 | 8/28 마감 작업으로 신설 |
| B-7 | 이미 구현된 기능 | 트래블 북·추억 지도를 신규처럼 서술 | `Story`·`TripMemorySummary` 구현 완료로 표기하고 남은 작업만 기술 |
| B-8 | 데이터 확충 운영 | 없음 | 전원 주 1시간 데이터 검수 운영 신설 |
| B-9 | `D-9`/`D-16` 표기 | D-day 표기 오용(기간을 D-n으로 씀) | 실제 일수(웹 10일 / 앱 7일)로 교체 |
| B-10 | 외부 API 쿼터 | "낮음" 위험으로만 처리 | 카카오·TourAPI 일일 한도 확인을 8/27 작업으로 명시 |
| B-11 | 이미지 검색 키 | 네이버·구글 키 미설정 사실 누락 | 현황과 대응 명시 |
| B-12 | 접근성 | 언급 없음 | 스토어 심사와 무관하지 않으므로 최소 기준 추가 |
| B-13 | 다국어 검수 | "100% 검수"만 있고 방법 없음 | 미번역 문자열 탐지 스크립트와 수용 기준 추가 |
| B-14 | 개인정보 처리방침 내용 | URL만 언급 | 수집 항목·보존기간·삭제 절차를 실제 코드 기준으로 초안화 |
| B-15 | 오프라인 동작 | 앱 요건에 없음 | 네트워크 단절 시 동작 기준 추가(리젝 사유 1위 대응) |
| B-16 | 문서 간 역할명 불일치 | 기획서 §9와 지침서 §2의 순서·명칭이 다름 | 역할 코드를 통일하고 두 문서에 동일 표 사용 |
| B-17 | 용어집 | 없음 | 부록에 추가 |

---

## 4. 좋았던 점 — 그대로 유지한 것

검토가 지적 위주가 되었으나, 다음은 잘 되어 있어 v6에서도 유지했다.

1. **스토어 심사 요건을 미리 챙긴 점.** 계정 삭제(5.1.1), 권한 목적 문구, 심사용 계정 — 방향이 정확하다. 시점만 앞당겼다.
2. **역할별 1단계/2단계 업무 분리.** 웹 → 앱으로 사람이 이동하는 구조를 잘 잡았다.
3. **`packages/shared` 도입.** 웹·앱이 타입과 API 클라이언트를 공유하는 설계가 옳다.
4. **12대 기능 명세의 구체성.** 특히 4대 카테고리 한국어 말하기, 8종 음식 칩, 사투리 위젯은 서비스 정체성이 잘 드러난다.
5. **페르소나 2인.** Emily와 켄타의 니즈가 기능과 정확히 연결된다. v6에서는 국내 여행자 페르소나를 하나 더했다.
6. **비밀번호 저장.** 문서 항목은 아니지만 코드가 `scrypt` + 계정별 salt + `timingSafeEqual`로 제대로 되어 있다. 손대지 않는다.

---

## 5. v6에서 달라진 구조

| | v5 | v6 |
| --- | --- | --- |
| 기획서 분량 | 약 314줄 | 약 1,400줄 |
| 지침서 분량 | 약 284줄 | 약 1,500줄 |
| 현재 상태 서술 | 없음 | **데모 감사 21건 신설** |
| 수치 근거 | 미표기 | **[실측]/[확정 사실]/[가설] 표기 도입** |
| 타임라인 | 격일 9개 지점 | **17일 전일 + 6인별 일일 과업** |
| 기능 명세 | 무엇을 만드나 | **현재 상태 → 실서비스 요건 → 수용 기준 → 담당** |
| 출시일 | 9/11 제출 | **iOS 9/15 전후 · Play 9/21 전후** (근거 명시) |
| 앱 범위 | 44화면 암묵 | **12화면 + 웹뷰** |

---

*이 보고서는 v6 문서와 함께 보관한다. 나중에 "왜 이렇게 정했는가"를 묻는 사람이 반드시 나온다.*
