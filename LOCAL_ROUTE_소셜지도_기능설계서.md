**버전** v1.0 · **작성일** 2026년 8월 29일 · **대상** LOCAL ROUTE 개발팀 6인

**이 문서** — "2D 지도에서 친구·팔로잉의 핀과 리뷰·댓글을 본다"는 아이디어를 실제로 만들 수 있는 형태로 다시 설계한다. 저장소 코드를 다시 감사한 결과, **필요한 것의 대부분이 이미 구현돼 있고 지도 화면에만 연결되어 있지 않다.** 아래는 그 연결 방법과, 콜드스타트를 피하기 위해 무엇을 바꿔야 하는지다.

# 1. 결론 먼저

| 원래 아이디어 | 이 문서의 제안 | 이유 |
|---|---|---|
| 친구·팔로잉의 위치를 지도에서 본다 | **동행(같이 온 사람)의 마지막 발자국** | 팔로우는 0건이고 재방문이 1~3년 주기라 자라지 않는다. 동행은 첫날부터 100% 존재한다 |
| 핀에 친구의 메시지·사진 | **핀 = 이미 있는 `Story`** + 3개 토글 + 스레드 댓글, **자동 번역** | `Story` 모델에 필요한 필드가 전부 있다. 번역은 네이버지도가 못 하는 자리다 |
| 지역별 사용자 레벨로 신뢰 형성 | **해무 걷기 + 로컬 음식 도감** | 레벨은 기여가 쌓인 뒤에야 신뢰가 된다. 안개와 도감은 **혼자서도 첫날부터 재미있다** |
| 마스코트·아바타가 지도 위를 이동 | **하지 않는다** | 아트·실시간 인프라 비용이 최대인데 여행 문제를 하나도 안 푼다. 부산 지역 마스코트는 지자체 IP다 |

> **핵심 문장 하나:**
> **한 번의 체크인이 세 가지를 동시에 일으킨다 — 해무가 걷히고, 도감이 채워지고, 다음 여행자를 위한 제보 핀이 남는다.**
> 사용자는 재미를 얻고, 우리는 지금 0%인 장소 데이터를 얻는다. 친구가 한 명도 없어도 성립한다.

# 2. 왜 "팔로우"가 아니라 "동행"인가

## 2.1 팔로우가 자라지 않는 구조적 이유

| 사실 | 수치 | 결과 |
|---|---|---|
| 현재 `Follow` 레코드 | **0건** | 시작점이 없다 |
| 현재 `Story` 레코드 | **0건** | 팔로우해도 볼 게 없다 |
| 부산 재방문 주기 | **1~3년** | 이번 여행에서 맺은 관계가 다음 접속까지 유지되지 않는다 |
| 생애 세션 수 | 체류 3~5일 × 하루 3~5회 = **15~25회** | 관계를 쌓을 접점 자체가 부족하다 |

소셜 그래프는 "자주 돌아오는 사용자"가 있어야 자란다. 우리 사용자는 정의상 그렇지 않다. **팔로우를 1번 기능으로 놓으면 영원히 빈 화면이 된다.**

## 2.2 동행은 콜드스타트가 없다

- 방한 관광객의 **82.9%가 개별여행(FIT)** 이고, 그중 대부분이 2인 이상 함께 온다.
- **같이 온 사람은 첫날부터 옆에 있다.** 초대할 사람을 찾을 필요가 없다.
- 실제 필요도 있다: 일행이 "나는 카페 갈게, 너는 시장 볼래?" 하고 흩어졌다가 다시 만난다. 지금은 이걸 카카오톡으로 하는데, 외국인 여행자에게는 그 카카오톡이 없다.

## 2.3 그리고 팔로우는 동행에서 자연 발생한다

```
동행 코드로 함께 다님  (여행 중, 3~5일)
        │
        ▼
여행 종료 화면: "Mei 님과 6곳을 함께 다녔습니다. 팔로우할까요?"
        │
        ▼
Follow 생성  ← 소셜 그래프가 '결과'로 생김
        │
        ▼
다음 여행에서 "팔로잉의 핀" 레이어가 실제로 채워져 있음
```

**팔로우 기능을 없애자는 게 아니다. 순서를 바꾸자는 것이다.** 팔로우는 M3에 두고, 동행을 M2에 둔다.

# 3. 저장소 코드 감사 결과 — 이미 있는 것

이 절이 이 문서에서 가장 중요하다. **새로 만들 게 거의 없다.**

## 3.1 해무 격자 — VisitVerification.gridCell

`server/src/routes/community.ts` 의 체크인 처리에서, 이미 이렇게 저장하고 있다.

```ts
const gridLatitude  = Math.round(latitude  * 200) / 200;   // 0.005° 단위
const gridLongitude = Math.round(longitude * 200) / 200;
gridCell: `${gridLatitude.toFixed(3)}:${gridLongitude.toFixed(3)}`
```

- 0.005° 위도 ≈ **557m**, 위도 35.18°에서 0.005° 경도 ≈ **455m** → 한 칸 약 **0.25㎢**
- 부산 면적 약 770㎢ → **부산 전체가 약 3,000칸**
- `server/src/workers/privacy-cleanup.ts` 가 일정 시간 후 원좌표를 격자로 뭉갠다

> **안개 지도에 새 테이블이 필요 없다.**
> `SELECT DISTINCT gridCell FROM VisitVerification WHERE sessionId = ?` 한 줄이 곧 "내가 걷어낸 해무"다.
> 그리고 이 값은 **이미 프라이버시 처리된 좌표**라, 동행·여행자에게 공유해도 정밀 위치가 새지 않는다. 설계가 우연히 맞아떨어진 게 아니라, 원래 이렇게 쓰라고 만들어진 필드다.

## 3.2 체크인 검증 — POST /api/places/:id/visits/verify

이미 동작하는 엔드포인트다. 다음을 전부 하고 있다.

| 검사 | 구현 상태 |
|---|---|
| 장소와 사용자 좌표의 거리 (`distanceM`) | `evaluateVisit()` 에서 판정 |
| 체류 시간 (`dwellMinutes`) | 동일 |
| 최근 7일 이내 방문만 허용 | 구현됨 |
| 미래 시각 방지 (+5분 허용) | 구현됨 |
| **같은 장소 하루 1회 제한** | `@@unique([sessionId, placeId, visitDate])` |
| 로컬 프로필·장소 점수 갱신 | `refreshLocalProfile()`, `refreshPlaceLocalScore()` |
| 이벤트 발행 | `recordEvent()` → `EventOutbox` |

> **위치 신뢰성과 스팸 방지가 이미 해결돼 있다.** 지도 핀을 "GPS로 검증된 방문만" 으로 제한하면, 어뷰징 대응을 새로 만들 필요가 없다.

## 3.3 핀 = Story

`Story` 모델에 이미 있는 필드:

| 필드 | 용도 |
|---|---|
| `placeId` | 핀이 붙을 장소 |
| `content` | 한 줄 메모 |
| `imageDataJson` | 사진 |
| `visibility` | `PUBLIC` / `FOLLOWERS` / `PRIVATE` ← **레이어 필터가 이미 모델링돼 있다** |
| `visitVerified` | GPS 검증 여부 ← 지도 표시 조건 |
| `areaLabel` | 지역 라벨 (정밀 좌표 대신 노출) |
| `moderationStatus` | `PUBLISHED` / `REVIEW` / `HIDDEN` / `REMOVED` |

그리고 `POST /api/stories`, `GET /api/stories`, `PATCH`, `DELETE`, `POST /api/stories/:id/report` 가 전부 구현돼 있다. `StoryReport` 모델과 모더레이션 엔드포인트 두 개도 있다.

> **핀 기능의 실체는 "Story를 지도에 그리는 것"이다.** 새 개념이 아니다.

## 3.4 동행 = TripMember

```prisma
model TripMember {
  tripId          String
  sessionId       String?
  role            String   @default("VIEWER")   // EDITOR | VIEWER
  inviteTokenHash String?  @unique
  expiresAt       DateTime?
  joinedAt        DateTime?
  revokedAt       DateTime?
}
```

그리고 `POST /api/trips/:tripId/members/invite` 와 `POST /api/collaboration/invites/:inviteToken/accept` 가 이미 동작한다.

> **동행 코드는 이 초대 토큰을 사람이 입력할 수 있는 6자리로 짧게 보여 주는 것**이 전부다.

## 3.5 이미 작성됐지만 어디에도 연결되지 않은 컴포넌트

`web/src/components/VisitFeedbackModal.tsx` (118줄)는 이미 이렇게 생겼다.

```ts
onSubmitFeedback?: (feedback: {
  rating: string; foodMatch: boolean; foreignEase: boolean; comment: string
}) => void;
```

`foodMatch`(음식이 맞았는가) · `foreignEase`(외국인이 이용하기 쉬웠는가) — **우리가 필요로 하는 제보 토글이 이미 코드로 존재한다.** 그런데 이 컴포넌트를 `import` 하는 파일이 저장소에 하나도 없다.

## 3.6 그리고 이 제보가 채우는 칸이 이미 Place 에 있다

| `Place` 컬럼 | 현재 채움률 | 제보 토글 |
|---|---|---|
| `hasEnglishMenu` | 낮음 | "영어 메뉴가 있나요?" |
| `foreignCardPayment` | 낮음 | "카드 결제가 되나요?" |
| `dietOptions` | **0%** | "채식·할랄 선택지가 있나요?" |
| `allergens` | **0%** | (핀 메모에서 추출) |
| `foreignAssistance` | 낮음 | "직원과 영어로 통했나요?" |

> **제보 핀은 소셜 기능이 아니라 데이터 수집 장치다.** 우리 최대 약점(장소 데이터 공백)을 사용자가 메워 준다. 이게 이 설계의 진짜 목적이고, 재미 요소는 그 대가로 주는 것이다.
---

# 4. 핵심 메커니즘 — 한 번의 체크인, 세 개의 보상

## 4.1 왜 보상을 묶는가

게임 디자인의 기본이다. **행동은 하나, 보상은 여러 개.** 사용자에게 세 가지 일을 시키면 아무것도 안 하지만, 한 번의 행동에 세 가지가 따라오면 그 행동을 반복한다.

```
                    ┌──────────────────────────┐
                    │   체크인 (GPS 자동 감지)   │
                    │  "이 가게에 계신가요?"      │
                    └────────────┬─────────────┘
                                 │  탭 1회 + 토글 3개 (3초)
        ┌────────────────────────┼────────────────────────┐
        ▼                        ▼                        ▼
┌───────────────┐      ┌──────────────────┐    ┌────────────────────┐
│ ① 해무가 걷힌다 │      │ ② 도감이 채워진다  │    │ ③ 제보 핀이 남는다   │
│               │      │                  │    │                    │
│ 즉각 시각 보상  │      │  수집욕           │    │  다음 여행자에게    │
│ 혼자서도 재미   │      │  밀면·돼지국밥…   │    │  도움이 된다        │
├───────────────┤      ├──────────────────┤    ├────────────────────┤
│ 우리가 얻는 것: │      │ 우리가 얻는 것:   │    │ 우리가 얻는 것:     │
│ gridCell 커버  │      │ 음식 카테고리별   │    │ hasEnglishMenu     │
│ 리지 · 실제 동선│      │ 실제 방문 분포    │    │ dietOptions 등     │
│               │      │                  │    │ 지금 0%인 칸        │
└───────────────┘      └──────────────────┘    └────────────────────┘
```

## 4.2 ① 해무 걷기

**규칙**

- 부산 전역이 해무로 덮여 있다. 바다는 덮지 않는다 — 걸어 다닐 곳이 아니다.
- 체크인 1회 → 해당 `gridCell` 과 인접 6칸(반경 약 1.1km)이 걷힌다.
- 걷힌 칸은 되돌아가지 않는다. **잃는 것이 없는 순수 획득**이라 스트레스가 없다.
- 계기판: `걷어낸 해무 %` · `탐험한 동네 n/16` · `남긴 제보 n건`

**왜 이게 재미있는가**
안개 걷기는 친구가 필요 없다. 첫 체크인부터 눈에 보이는 변화가 생긴다. 그리고 "3일 만에 부산의 4%밖에 못 봤다"는 사실 자체가 재방문 동기가 된다.

**계기 단위를 두 층으로 두는 이유**
격자(약 3,000칸) 기준으로는 3일 여행에 1~2%밖에 안 걷힌다 — 숫자가 초라하다. 그래서 **동네(행정동, 부산 약 200개)와 구·군(16개)** 을 함께 보여 준다. "부산 16개 구 중 5곳"은 달성감이 있다.

| 단위 | 개수 | 용도 |
|---|---|---|
| 격자 `gridCell` | 약 3,000 | 지도 위 안개 텍스처, 정밀도 |
| 행정동 | 약 200 | 진행률 계기 |
| 구·군 | 16 | 배지·수집 목표 |

## 4.3 ② 로컬 음식 도감

**규칙**

- `Place` 에 `dexTag` 컬럼 하나를 추가한다 (`milmyeon` / `gukbap` / `hotteok` / `eomuk` / `pajeon` / …).
- 해당 태그를 가진 장소에 처음 체크인하면 도감 칸이 열린다.
- 도감 칸에는 **그 음식의 원재료·알레르기·채식 가능 여부**가 함께 붙는다.

> **도감이 단순한 수집 요소가 아닌 이유가 여기 있다.** "밀면 획득 🍜" 옆에 "밀 함유 · 육수는 가게마다 다름 · 채식 불가" 가 붙는다. 재미 요소가 곧 우리의 1번 차별점(식이 판정)을 사용자에게 학습시키는 장치가 된다.
>
> 도감은 **별도 테이블이 필요 없다.** `VisitVerification` ⋈ `Place.dexTag` 로 유도한다.

## 4.4 ③ 제보 핀

**작성 흐름 (30초 이내)**

1. 체크인이 확인되면 시트가 뜬다
2. 토글 3개 — 영어 메뉴 / 카드 결제 / 채식·할랄 · 각각 `있음 · 없음 · 모름`
3. (선택) 한 줄 메모, 사진 1장
4. 저장 → 지도에 내 핀이 꽂힌다

**"모름"을 1급 선택지로 두는 이유**
모르면 모른다고 말하게 해야 데이터가 정직해진다. 이건 우리가 이미 채택한 알레르기 **"확인 필요(⚠️)" 정책**과 같은 원칙이다. 억지로 답하게 만들면 무슬림·알레르기 사용자를 위험에 빠뜨린다.

**핀 신뢰 배지**
핀 상세에 이렇게 표시한다.

```
🍲 돼지국밥 골목
   ✕ 돼지 포함  ✕ 영어 메뉴  ✓ 직원 영어
   여행자 4명이 확인 · 가장 최근 확인 3일 전
```

레벨이나 등급이 아니라 **"몇 명이 언제 확인했는가"** 라는 직접 신호다. 사용자가 1명일 때는 "1명이 확인 · 오늘"이라고 정직하게 쓰면 되고, 이 표현은 사용자가 늘어도 그대로 쓸 수 있다.

## 4.5 자동 번역 — 이 설계에서 가장 방어력 있는 부분

**문제:** 부산 방문객의 국적 1~3위는 대만(18.9%) · 중국(15.4%) · 일본(14.9%) 으로 절반이다. 이들이 서로의 핀을 못 읽는다. 그리고 네이버지도의 가장 큰 약점이 정확히 이것 — **외국인에게 리뷰가 전부 한국어로 보인다.**

**설계**

- 핀과 댓글은 작성 언어를 `language` 로 저장하고, 조회 시 뷰어 언어로 번역해 보여 준다.
- 원문은 항상 접근 가능하게 둔다 (`원문 보기` 토글). 번역이 틀렸을 때 사용자가 확인할 수 있어야 한다.
- 번역 결과는 `(원문 언어) → (뷰어 언어)` 로 캐시한다. `PlaceTranslation` 파이프라인의 구조를 그대로 쓴다.
- **한국인이 쓴 리뷰도 번역 대상이다.** 이게 네이버지도 대비 결정적 우위다.

| | 네이버지도 | 트리플 코리아 | 크리에이트립 | **LOCAL ROUTE** |
|---|---|---|---|---|
| 장소 리뷰 | 한국어만 | 자체 콘텐츠 | 자체 콘텐츠 | **여행자 핀 + 자동 번역** |
| 여행자 간 언어 | — | — | — | **번체·간체·일본어·영어 상호 번역** |

---

# 5. 위치 공유 — 실시간이 아니라 "마지막 발자국"

## 5.1 상시 위치 공유를 하지 않는 이유

| 문제 | 내용 |
|---|---|
| iOS 심사 | 백그라운드 위치 권한(`Always`)은 심사에서 사용 목적 소명을 요구하며, 반려·지연 사유 상위권이다. **9/11 출시 일정에 넣을 수 없다.** |
| Android | `ACCESS_BACKGROUND_LOCATION` 은 Play 정책상 별도 선언·심사 대상 |
| 배터리 | 상시 위치 + 상시 연결은 여행자에게 치명적이다. 보조배터리를 들고 다니는 사용자에게 배터리를 먹는 앱은 삭제 1순위 |
| 개인정보 | 외국인의 실시간 위치를 상시 수집·전송하는 것은 위험 대비 효용이 낮다 |
| 인프라 | WebSocket 상시 연결 유지 = 서버 비용·복잡도 증가 |

## 5.2 대신 이렇게 한다

**체크인·핀 작성 시점의 `gridCell` 만 동행에게 공유한다.**

```
Mei · 센텀시티 근처 · 2시간 전
```

- 위치 권한은 **사용 중에만(When In Use)** — 심사 부담이 사실상 없다
- 배터리 영향 없음
- 정밀 좌표가 아니라 **약 500m 격자**라 프라이버시 문제도 없다 (그리고 이미 그렇게 저장되고 있다)
- 동행을 다시 만나는 데는 이 정도면 충분하다. 정확히 어디 있는지는 결국 서로 연락해서 정한다

**실시간 동기화도 WebSocket이 필요 없다.**
일정 생성 진행률에 이미 쓰고 있는 **SSE**(`server/src/routes/trips.ts` 의 `text/event-stream`, 웹은 `EventSource`)를 그대로 재사용한다. 동행 인원은 2~5명이고 이벤트는 몇 분에 한 번이라, 단방향 스트림으로 충분하다.

## 5.3 프라이버시 기본값

| 항목 | 기본값 |
|---|---|
| 내 핀 공개 범위 | `PUBLIC` (선택 시 `PRIVATE`) |
| 내 발자국 공유 | **동행에게만**, 여행 기간 동안만 |
| 동행 만료 | `TripMember.expiresAt` — 여행 종료 + 7일 후 자동 만료 |
| 위치 정밀도 | 격자(약 500m) — 원좌표는 워커가 뭉갠다 |
| 위치 공유 끄기 | 언제든 1탭, 이미 공유된 발자국도 삭제 |
| 계정 | **불필요.** 익명 세션으로 전부 동작 (한국 휴대폰 인증은 외국인에게 불가) |
---

# 6. 데이터 모델 — 신규는 1개 테이블 + 6개 컬럼

## 6.1 추가할 것

```prisma
// 핀에 지도 좌표와 제보 토글을 붙인다
model Story {
  // ... 기존 필드 유지 ...
  gridCell    String?                    // 신규 · 지도 렌더링 단위 (VisitVerification 과 동일 규칙)
  factsJson   String   @default("{}")    // 신규 · {"englishMenu":"YES","card":"NO","diet":"UNKNOWN"}
  language    String   @default("KO")    // 신규 · 원문 언어
  comments    StoryComment[]             // 신규 관계
  @@index([gridCell, moderationStatus])  // 신규 · 지도 bbox 조회용
}

// 핀 스레드 댓글 — 유일한 신규 테이블
model StoryComment {
  id               String           @id @default(cuid())
  story            Story            @relation(fields: [storyId], references: [id], onDelete: Cascade)
  storyId          String
  author           AnonymousSession @relation(fields: [authorSessionId], references: [id])
  authorSessionId  String
  body             String
  language         String           @default("KO")
  moderationStatus String           @default("PUBLISHED")
  createdAt        DateTime         @default(now())
  @@index([storyId, moderationStatus, createdAt])
}

model Place {
  // ... 기존 필드 유지 ...
  dexTag String?   // 신규 · 로컬 음식 도감 태그 (milmyeon | gukbap | hotteok | eomuk | pajeon | ...)
}

model Trip {
  // ... 기존 필드 유지 ...
  partyCode String? @unique   // 신규 · 사람이 입력하는 6자리 동행 코드
}

model TripMember {
  // ... 기존 필드 유지 ...
  lastSeenGridCell String?     // 신규 · 마지막 발자국 (정밀 좌표 아님)
  lastSeenAt       DateTime?   // 신규
}
```

## 6.2 추가하지 않는 것 (그리고 그 이유)

| 만들고 싶어지는 테이블 | 만들지 않는 이유 |
|---|---|
| `FogCell` (탐험한 격자) | `VisitVerification.gridCell` 이 이미 그것이다. `SELECT DISTINCT` 로 유도 |
| `DexEntry` (도감 보유) | `VisitVerification ⋈ Place.dexTag` 로 유도. 중복 저장은 정합성 부채만 만든다 |
| `Party` / `PartyMember` | `Trip` + `TripMember` 가 이미 그것이다 |
| `UserLevel` / `RegionLevel` | `LocalProfile.grade` · `trustScore` 가 이미 있다. 그리고 M3까지 쓰지 않는다 |
| `PinReaction` (좋아요) | 좋아요는 콜드스타트에서 항상 0이라 사기를 꺾는다. 대신 "확인한 여행자 수"를 쓴다 |

# 7. API 설계

## 7.1 신규 (5개)

| 메서드 · 경로 | 응답 | 비고 |
|---|---|---|
| `GET /api/map/fog` | `{ cells: string[], areaCount, districtCount, totalCells }` | `?scope=me · party · travelers` |
| `GET /api/map/pins` | `{ pins: [...] }` | `?bbox=&layers=me,party,travelers&lang=` |
| `GET /api/stories/:id/comments` | `{ comments: [...] }` | `?lang=` 로 번역본 동봉 |
| `POST /api/stories/:id/comments` | `201 { comment }` | 세션 필요 |
| `POST /api/party/:code/join` | `200 { tripId, members }` | 6자리 코드로 동행 참여 |

## 7.2 확장 (기존 엔드포인트에 필드 추가)

| 경로 | 추가 |
|---|---|
| `POST /api/places/:id/visits/verify` | 응답에 `clearedCells: string[]`, `dexUnlocked` (없으면 null), `progress: {fogPct, areas, districts}` |
| `POST /api/stories` | 요청에 `facts`, `language`; 저장 시 `gridCell` 자동 채움 |
| `GET /api/stories` | `?lang=` 시 `translatedContent` 동봉, `?gridCell=` 필터 |
| `POST /api/trips/:tripId/members/invite` | 응답에 `partyCode` (6자리) 포함 |
| `GET /api/trips/:tripId` | `members[].lastSeenGridCell`, `lastSeenAt` 포함 |

## 7.3 재사용

| 용도 | 기존 자산 |
|---|---|
| 동행 실시간 갱신 | `trips.ts` 의 SSE (`text/event-stream`) + 웹 `EventSource` |
| 지도 렌더링 | `web/src/components/KakaoMap.tsx` (Kakao Maps SDK 로더 포함) |
| 핀 작성 UI | `web/src/components/VisitFeedbackModal.tsx` (작성돼 있으나 미연결) |
| 신고·숨김 | `POST /api/stories/:id/report`, `PATCH /api/moderation/reports/:id` |
| 번역 캐시 구조 | `PlaceTranslation` 파이프라인 |

## 7.4 지도 조회 성능

`GET /api/map/pins` 는 지도를 움직일 때마다 호출된다. 다음을 지킨다.

- **bbox 필수.** 파라미터 없이 전체 조회를 허용하지 않는다.
- 응답 상한 500건. 초과 시 격자 단위로 묶어 `{gridCell, count}` 클러스터로 반환한다.
- `@@index([gridCell, moderationStatus])` 로 커버.
- 안개(`/api/map/fog`)는 세션당 결과가 자주 안 바뀌므로 **60초 캐시 + ETag**.
- 번역은 조회 시점 생성이 아니라 **작성 시점에 미리 생성**해 캐시한다. 지도 팬 중에 번역 API를 때리면 안 된다.

---

# 8. 화면 설계

기존 `기능·화면 상세설계서`의 P-40 다음 번호를 이어 쓴다.

## P-41 · 해무 지도 (탐험 지도)

| 항목 | 내용 |
|---|---|
| 목적 | 내가 어디까지 봤는지 한눈에 보고, 다음에 갈 곳을 정한다 |
| 진입 | 하단 탭 `지도` · 홈 탐험 카드 · 여행 종료 요약 |
| 이탈 | 핀 상세(P-43) · 체크인(P-42) · 장소 상세(기존 P-14) |
| 레이어 | `나`(청록) · `동행`(주황, 코드 참여 시) · `여행자`(회백) — 체크박스 3개, 같은 지도의 필터일 뿐 별도 화면이 아니다 |
| 계기 | 걷어낸 해무 % · 탐험한 동네 n/약 200 · 방문한 구 n/16 · 남긴 제보 n건 |
| API | `GET /api/map/fog`, `GET /api/map/pins` |
| 빈 상태 | 전면 해무 + "가까운 곳부터 걷어 보세요" + 주변 장소 3개 카드 |
| 오프라인 | 마지막 조회 결과를 캐시해 보여 주고 상단에 `오프라인 · 마지막 갱신 n분 전` 배지 |
| 반응형 | 모바일: 지도 전체화면 + 하단 시트(계기·레이어). 데스크톱: 지도 + 우측 레일 |
| 접근성 | 핀은 `<button>`, 안개는 장식(canvas `aria-hidden`이 아니라 요약 `aria-label`), 계기는 텍스트로도 읽힘 |

## P-42 · 체크인 시트

| 항목 | 내용 |
|---|---|
| 목적 | 30초 안에 방문을 확정하고 제보를 남긴다 |
| 진입 | 지도 핀 · 장소 상세 · **일정 항목 도착 시 자동 제안** |
| 구성 | ① GPS 확인 결과(거리·체류) ② 보상 미리보기("해무 7칸 · 도감 밀면") ③ 토글 3개(있음/없음/모름) ④ 한 줄 메모·사진(선택) |
| API | `POST /api/places/:id/visits/verify` → `POST /api/stories` |
| 실패 | 거리 초과 → "장소에서 조금 떨어져 있어요. 가까이 가서 다시 시도하거나 건너뛰세요" · 하루 중복 → "오늘은 이미 확인했어요" |
| 원칙 | **토글은 전부 선택 사항.** 하나도 안 고르고 저장할 수 있다 |

## P-43 · 핀 상세 · 스레드

| 항목 | 내용 |
|---|---|
| 목적 | 다른 여행자의 제보를 읽고, 묻고, 답한다 |
| 구성 | 작성자·원문 언어·경과 시간 → **번역된 본문** → 원문(접기) → 제보 배지 3개 → 확인 인원·최근 확인일 → 댓글 스레드 |
| 조작 | `원문 보기` 토글 · 댓글 작성 · 신고 · 장소 상세로 이동 · (M3) 작성자 팔로우 |
| API | `GET /api/stories?placeId=&lang=`, `GET/POST /api/stories/:id/comments`, `POST /api/stories/:id/report` |
| 빈 상태 | "아직 이 장소에 남겨진 핀이 없습니다" + `여기 체크인하고 첫 핀 남기기` |

## P-44 · 동행 참여

| 항목 | 내용 |
|---|---|
| 목적 | 같이 온 사람과 지도를 공유한다 |
| 구성 | 내 코드 표시(6자리) + 공유 버튼 · 코드 입력란 · 참여 중인 동행 목록(마지막 발자국) |
| API | `POST /api/trips/:tripId/members/invite`, `POST /api/party/:code/join` |
| 원칙 | **계정·전화번호 불필요.** 코드만으로 참여 |
| 만료 | 여행 종료 + 7일 후 자동 만료. 화면에 만료일 명시 |
| 종료 시 | "Mei 님과 6곳을 함께 다녔습니다" → (M3) 팔로우 제안 |

## P-45 · 로컬 음식 도감

| 항목 | 내용 |
|---|---|
| 목적 | 수집 동기를 주고, 동시에 음식별 식이 정보를 학습시킨다 |
| 구성 | 8~12칸 그리드. 미획득은 `???` · 획득은 이름·아이콘·**원재료/알레르기/채식 가능 여부** |
| API | `GET /api/map/fog` 응답에 동봉 (별도 호출 없음) |
| 확장 | 구·군 배지(16개)는 M3 |

## 화면 연결

```
        홈 (현장 도구 바)
              │
        ┌─────┴─────┐
        ▼           ▼
   P-41 해무 지도   장소 상세(P-14)
        │  ▲            │
   ┌────┼──┴────────────┘
   ▼    ▼
P-43   P-42 체크인 ──→ 보상 애니메이션 ──→ P-41 로 복귀
핀상세    │
   │      └──→ P-45 도감 (신규 획득 시)
   ▼
P-44 동행 참여
```
---

# 9. 사용자가 0명일 때 화면은 어떻게 보이는가

이 절을 통과하지 못하는 소셜 기능은 출시하면 안 된다. **출시 첫날 사용자는 1명이다.**

| 화면 | 사용자 0명일 때 | 성립하는가 |
|---|---|---|
| P-41 해무 지도 · `나` 레이어 | 전면 해무. 체크인 1회에 즉시 걷힘 | ✅ **혼자서 완결** |
| P-41 · `여행자` 레이어 | 핀 0건. 레이어 자체를 숨기지 않고 "아직 여행자 제보가 없습니다 — 첫 번째가 되어 주세요" | ✅ 정직하고 행동 유도 |
| P-41 · `동행` 레이어 | 코드 미참여 시 잠금 표시(`· 코드 필요`) | ✅ 빈 화면이 아니라 잠금이라 실패로 안 읽힘 |
| P-42 체크인 | 완전히 혼자 동작 | ✅ |
| P-43 핀 상세 | 내 핀만 보임. 댓글 0 | ✅ |
| P-45 도감 | 8칸 전부 `???` → 첫 체크인에 1칸 열림 | ✅ **수집욕은 혼자서 작동** |
| (참고) 팔로우 피드 | 팔로잉 0 → **영원히 빈 화면** | ❌ **그래서 M3로 미룬다** |

> 이 표가 이 문서의 검증이다. **팔로우만 유일하게 통과하지 못한다.** 나머지는 전부 사용자 1명으로 성립한다.

## 9.1 첫 100명 시드 전략

제보 핀이 0건이면 `여행자` 레이어가 비어 있다. 이걸 채우는 정직한 방법:

1. **팀 6명이 직접 채운다.** 부산 주요 상권 100곳에 실제로 가서 체크인하고 3토글을 채운다. 2인 1조 3일이면 가능하다. 이건 "가짜 데이터"가 아니라 실제 방문 기록이다.
2. **가짜 계정·가짜 리뷰는 절대 만들지 않는다.** 무슬림·알레르기 사용자가 이 데이터를 믿고 음식을 먹는다. 한 건이라도 지어내면 그건 안전 문제다.
3. `localScoreSource` 를 `SEED` / `VERIFIED_COMMUNITY` 로 구분하는 필드가 이미 있다. **핀에도 같은 구분을 표시한다** — "운영팀 확인" vs "여행자 4명 확인".

# 10. 어뷰징과 모더레이션

기존 자산으로 대부분 커버된다.

| 위험 | 대응 | 상태 |
|---|---|---|
| 가짜 체크인 (안 가고 핀 남기기) | GPS 거리·체류시간 검증 (`evaluateVisit`) | **구현됨** |
| 도배 | 같은 장소 하루 1회 (`@@unique`) | **구현됨** |
| 유해 게시물 | `StoryReport` + `moderationStatus` + 모더레이션 API 2개 | **구현됨** |
| 사진 악용 | 핀 사진은 검증된 방문에만 허용 | 정책 |
| 개인정보 노출 | 좌표는 격자로만 저장·노출, 워커가 원좌표 삭제 | **구현됨** |
| 번역 오역으로 인한 위험 | 원문 항상 병기 · 알레르기 관련은 "확인 필요" 우선 | 정책 |
| 스토킹 | 발자국은 동행에게만·격자 단위·여행 기간 한정·언제든 끄기 | 설계 |

> 한 가지 정책을 명시한다. **지도에 표시되는 핀은 `visitVerified = true` 인 것만이다.** 검증 없이 작성된 스토리는 피드에는 남되 지도에는 안 뜬다. 이 한 줄이 지도 스팸의 대부분을 원천 차단한다.

# 11. 단계별 범위와 공수

| 단계 | 기한 | 범위 | 레이어 | 담당 · 공수 |
|---|---|---|---|---|
| **M1** | 9/4 웹 | 해무 지도 · 체크인 · 제보 핀 · 핀 상세 · 자동 번역 | 나 / 여행자 | FE 3일 · BE 2일 · DATA 1일 |
| **M2** | 9/11 앱 | 동행 코드 · 동행 레이어 · 마지막 발자국(SSE) · 도감 | + 동행 | FE 2일 · APP 2일 · BE 2일 |
| **M3** | 출시 후 | 팔로우(동행에서 파생) · 구·군 배지 · 지역 등급 | + 팔로잉 | 미정 |

## M1 작업 분해 (9/4까지)

| # | 작업 | 담당 | 일 |
|---|---|---|---|
| 1 | `Story.gridCell / factsJson / language`, `StoryComment`, `Place.dexTag` 마이그레이션 | BE | 0.5 |
| 2 | `GET /api/map/fog`, `GET /api/map/pins` (bbox·클러스터·ETag) | BE | 1 |
| 3 | `visits/verify` 응답에 `clearedCells` · `progress` 추가 | BE | 0.5 |
| 4 | 핀 댓글 API 2개 + 작성 시점 번역 캐시 | BE | 1 |
| 5 | 지도 안개 오버레이 (Kakao 지도 위 격자 폴리곤) | FE | 1.5 |
| 6 | 레이어 스위치 · 계기판 | FE | 0.5 |
| 7 | `VisitFeedbackModal` 을 체크인 시트로 연결 | FE | 0.5 |
| 8 | 핀 상세 · 스레드 · 원문 토글 | FE | 0.5 |
| 9 | `Place.dexTag` 시드 (8~12종) · 음식별 알레르기·채식 정보 | DATA | 1 |

**합계 약 8인일.** 6인 팀에서 2~3명이 붙으면 3~4일.

## 성능·품질 수용 기준

| 항목 | 기준 |
|---|---|
| 지도 최초 렌더 | 3G 환경에서 2.5초 이내 |
| 지도 팬·줌 | 45fps 이상 (안개는 캔버스 1장, DOM 노드 아님) |
| `GET /api/map/pins` | p95 300ms 이내, 응답 500건 상한 |
| 체크인 → 안개 반영 | 낙관적 업데이트로 **즉시**, 서버 실패 시 롤백 + 토스트 |
| 오프라인 | 마지막 안개·핀 캐시 표시 + 갱신 시각 배지 |
| 접근성 | 핀 키보드 접근·포커스 표시, 계기 텍스트 대체, 색만으로 레이어 구분하지 않음(모양 병행) |
| 번역 | 원문 항상 접근 가능. 번역 실패 시 원문 표시 + "번역할 수 없습니다" |

# 12. 하지 않는 것

| 아이디어 | 결정 | 이유 |
|---|---|---|
| 실시간 아바타·마스코트 이동 | **하지 않음** | 캐릭터 아트 + 애니메이션 + 실시간 위치 동기화가 최소 1.5명 × 3주. 여행 문제를 하나도 안 푼다. 부산 지역 마스코트는 지자체 IP라 상업 이용에 허가가 필요하다 |
| 상시 백그라운드 위치 공유 | **하지 않음** | iOS 심사 지연 위험 · 배터리 · 개인정보. 대신 마지막 발자국(§5) |
| 지역별 사용자 레벨 | **M3로 연기** | 기여가 쌓이기 전엔 전원 Lv.1이라 신뢰 신호가 0. 대신 "여행자 n명 확인 · 최근 n일 전"이라는 직접 신호를 쓴다 |
| 팔로우 피드 | **M3로 연기** | 사용자 0명일 때 유일하게 빈 화면이 되는 기능 (§9) |
| 좋아요 · 랭킹 | **하지 않음** | 콜드스타트에서 항상 0이라 오히려 사기를 꺾는다 |
| WebSocket 실시간 동기화 | **하지 않음** | SSE로 충분하다. 이미 있다 |

# 13. 요약

1. **새 기능이 아니라 연결이다.** `gridCell`(안개) · `VisitVerification`(검증) · `Story`(핀) · `TripMember`(동행) · `VisitFeedbackModal`(제보 UI)이 전부 이미 있고, 지도에만 연결돼 있지 않다. 신규는 **테이블 1개 + 컬럼 6개 + API 5개**다.

2. **팔로우가 아니라 동행으로 시작한다.** 팔로우는 사용자가 쌓여야 자라지만, 같이 온 사람은 첫날부터 옆에 있다. 팔로우는 여행이 끝날 때 동행에서 자연히 파생된다.

3. **한 번의 체크인이 세 개의 보상을 준다.** 해무가 걷히고, 도감이 채워지고, 제보 핀이 남는다. 사용자는 재미를 얻고 우리는 지금 0%인 `dietOptions` · `hasEnglishMenu` 를 얻는다.

4. **재미 요소가 차별점을 강화한다.** 도감 칸에 알레르기·채식 정보가 붙고, 핀에 자동 번역이 붙는다. 게임 장치가 우리의 1번 차별점(식이 판정)과 네이버지도의 최대 약점(한국어 리뷰)을 동시에 공략한다.

5. **위치는 실시간이 아니라 마지막 발자국이다.** 심사·배터리·프라이버시 문제를 전부 피하면서, 동행이 서로를 찾는 실제 용도는 그대로 만족한다.

## 다음에 결정할 것

- `Place.dexTag` 시드 목록 — 부산 음식 8종으로 갈지 12종으로 갈지, 그리고 각 음식의 알레르기·채식 정보를 누가 검증할지
- 첫 100곳 현장 시드를 팀 6명이 직접 돌지 여부 (2인 1조 3일)
- 안개 진행률 계기의 기본 단위 — 격자(정밀하지만 숫자가 초라함) vs 동네(달성감 있음). 현재 제안은 둘 다 표시
